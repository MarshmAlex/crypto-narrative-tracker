/** @type {import('next').NextConfig} */
const nextConfig = {
  // Allow serving static JSON from /public/data/
  // The backend writes narratives.json there after each run
};

module.exports = nextConfig;
